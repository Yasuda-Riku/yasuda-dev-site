# Cloudflare Pages custom response headers.
# https://developers.cloudflare.com/pages/configuration/headers/

# The Tetris course embeds jar bytes directly in /learn/tetris/step-01/
# runtime.js and loads them into CheerpJ's /str/ virtual filesystem, so
# no .jar HTTP requests actually happen. No per-extension headers needed.
