base case 2 つ: depth == 0 → return evaluate(AI); 合法手が無く相手もパスなら終局 → 同じく return evaluate(AI);
maximizing = (player == AI); 最大化か最小化かは player で決まる。int best = maximizing ? Integer.MIN_VALUE : Integer.MAX_VALUE;
再帰: int score = minimax(depth - 1, opponent); Math.max または Math.min で best を更新。snapshot/restore を忘れない。
