# 1行1ヒント。# から始まる行はコメント（無視されます）。
外側の for は row を 0 から ROWS 未満まで、内側の for は col を 0 から COLS 未満まで回します。
各マスの左上の座標は col * CELL と row * CELL で計算できます。
隣のマスと1px ずつ隙間を空けるため、描画は (x + 1, y + 1, CELL - 2, CELL - 2) を Color.DARK_GRAY で塗るのが定番です。
