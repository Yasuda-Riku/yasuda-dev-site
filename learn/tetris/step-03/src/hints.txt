TODO 1: accumulator += 0; の 0 を dt に変えるだけ。これで「経過時間を貯金する」になります。
TODO 2: while の中で blockRow++; を書きます。accumulator -= DROP_SECONDS; の次の行。
TODO 3: if (blockRow >= ROWS) blockRow = 0; の 1 行を while ループの中に書く。下端まで行ったら上に戻ります。
