TODO 1a: board[blockRow][blockCol] = 1; の 1 行。2 次元配列への代入です。
TODO 1b: blockRow = 0; と blockCol = COLS / 2 - 1; の 2 行を並べる（上から中央寄りに戻す）。
TODO 2: if (board[row][col] != 0) return false; の 1 行。境界チェックの後ろに追加します。
